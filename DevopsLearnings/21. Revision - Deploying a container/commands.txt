
- Install Docker on a Virtual machine Linux
- Place docker file in the solution directory in visual studio
- Publish code in visual studio and copy the publish code onto the Linux machine using a tool called WinSCP, along with dockerfile

FROM mcr.microsoft.com/dotnet/aspnet:8.0
WORKDIR /app
COPY  . .
EXPOSE 80
ENTRYPOINT ["dotnet", "SqlApp.dll"]

- Publish .NET application onto the your local machine and copy the publish content to Linux machine
- Create custom custom image
- Push image to container registry
- Use that image and run a container in azure container apps or K8s

1. Install Docker on Ubuntu via the following URL
https://docs.docker.com/engine/install/ubuntu/
 
2. build a custom image
 
sudo docker build -t sqlapp .

sudo docker images

Install Docker Engine on Ubuntu
Jumpstart your client-side server applications with Docker Engine on Ubuntu. This guide details prerequisites and multiple methods to install Docker Engine on Ubuntu.
 
1. Install the Azure CLI via the following URL
https://docs.microsoft.com/en-us/cli/azure/install-azure-cli-linux?pivots=apt
 
2. Push image to container registry
 - 2.1 First log into the Azure container registry
    sudo az acr login --name appregistry300030 --username appregistry300030 --password
     2oWgrdyo74F7NQFhLTepqOnQHnWF9EDs1liUoPMhb7YNviimT20GJQQJ99CDACGhslBEqg7NAAACAZCROXAE

  - 2.2 Then tag your image
 
     sudo docker tag sqlapp appregistry300030.azurecr.io/sqlapp
 
   - 2.3 Then push the image to the Azure Container registry
 
     sudo docker push appregistry300030.azurecr.io/sqlapp

Install the Azure CLI on Linux | Microsoft Learn
Learn how to install and run the Azure CLI on Linux manually. You can install the Azure CLI on Linux computers with one command or a step-by-step process.

-- Create release pipeline and cli task with below code
az container create -g devops-grp --name appinstance20030 
--cpu 1 --memory 1  --ports 80 --ip-address Public 
--image registry57767.azurecr.io/sqlappdocker:latest --registry-username registry57767 --registry-password XeVWwKmmKD2IAS34A8B8i7E+MWJlneju
 
az container create -g template-grp --name appinstance20030345 
--cpu 1 --memory 1  --ports 80 --ip-address Public 
--os-type Windows --image localregistryaruna124.azurecr.io/sqlapp:latest --registry-username localregistryaruna124 --registry-password DXnjiZbrb0EYafq2LCDM9vtvTxdWakQOqX6HA7EdVIjSm4087IGsJQQJ99CFACGhslBEqg7NAAACAZCRZevu
